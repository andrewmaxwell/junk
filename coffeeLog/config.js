// After deploying apps-script/Code.gs, paste its /exec URL here to connect
// every device. Alternatively, enter it in the app's Settings on each device.
window.COFFEE_LOG_CONFIG = { endpoint: '' };
